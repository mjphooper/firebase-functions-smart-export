// Build config for the CLI/shared output (tsc); src/runtime is bundled separately by esbuild.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": false,
    "outDir": "",
    "rootDir": "./src"
  },
  "include": [
    "src/cli",
    "src/shared"
  ],
  "exclude": [
    "node_modules",
    "test",
    "src/runtime"
  ]
}
